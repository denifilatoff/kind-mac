  - role: worker
    image: kindest/node:${K8S_VERSION}
    extraMounts:
      - hostPath: ${STORAGE_ROOT}
        containerPath: ${STORAGE_ROOT}
      - hostPath: /var/run/docker.sock
        containerPath: /var/run/docker.sock
